// Shared between the hero's letter-hover effect (components/hero.tsx) and
// the custom cursor (components/custom-cursor.tsx) so both cycle through
// the exact same 4 colors, in the same order, at the same speed.
export const RAINBOW_COLORS = ["#EC5149", "#2C67F6", "#EF87D1", "#46B13D"];
export const RAINBOW_STEP_MS = 800;
